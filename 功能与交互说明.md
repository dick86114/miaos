# 妙生（miaos）功能与交互说明

## 一、应用概述

妙生是一款运行在 macOS 上的本地 AI 生图桌面应用（基于 Electron），支持连接多种 AI 图像生成 API 供应商（内置 Grsai，也支持自定义 OpenAI 兼容接口）。核心模式有两种：**快速生图**（单张、一次性生成）和**项目生图**（带版本树/时间轴的迭代式探索）。所有数据存储在本地（localStorage + 本地文件），不需要账号登录。

---

## 二、全局导航

应用采用左侧固定导航栏 + 右侧内容区的结构。导航项如下：

| 导航项 | 路由 | 功能 |
|---|---|---|
| 快速生图 | `/generate` | 输入提示词直接生成单张图片 |
| 项目列表 | `/projects` | 查看所有项目，进入项目工作区 |
| 生成历史 | `/history` | 查看所有快速生图的历史记录 |
| 模型管理 | `/models` | 管理 API 供应商、模型、API Key、测试连接 |
| 设置 | `/settings` | 查看版本、检查/下载/安装软件更新 |

侧边栏底部显示当前应用版本号。

快捷键：
- `⌘ + Enter`：在生图输入框内触发生成
- `⌘ + R`：刷新页面

---

## 三、模型管理页（供应商配置）

### 3.1 供应商列表
- 显示所有已配置的 API 供应商卡片列表
- 默认内置「Grsai」供应商，预设 10 个模型（默认启用 gpt-image-2、nano-banana-2、nano-banana-pro）
- 每个供应商卡片显示：名称、API 类型标签、API 地址、是否默认供应商

### 3.2 供应商操作
- **添加供应商**：填写名称、API 地址（endpoint）、API Key、类型（Grsai / OpenAI 兼容）
- **编辑供应商**：修改以上信息
- **删除供应商**：二次确认后删除；若删除的是默认供应商，自动将第一个供应商设为默认
- **设为默认**：标记该供应商为默认选中项
- **测试连接**：调用 API 验证 endpoint + API Key 是否可用，返回成功/失败提示
- **获取模型列表**：从 API 拉取该供应商可用的模型列表

### 3.3 模型管理
- 每个供应商下有模型列表，显示模型 ID/名称
- 每个模型可单独启用/禁用（复选框/开关）
- 支持手动添加自定义模型（输入模型 ID）
- 支持移除自定义模型
- Grsai 供应商提供"刷新内置模型列表"功能

### 3.4 文本模型配置
- 配置一个文本模型（endpoint + API Key + model name）
- 用途：优化提示词功能（见快速生图页）

---

## 四、快速生图页

### 4.1 输入区
- **提示词输入框**：多行文本输入，支持 Shift+Enter 换行
  - 支持粘贴图片：通过系统剪贴板粘贴图片（⌘+V），粘贴后图片显示在输入框内上方作为参考图（图生图模式）
  - 粘贴参考图后自动切换为图生图模式，右上角显示"参考图已粘贴，将使用图生图模式"提示
  - 已粘贴的参考图可点击 X 按钮移除
- **参数选择器**（输入框下方工具栏）：
  - **模型选择**：下拉选择当前启用的模型（从所有启用供应商的启用模型中聚合）
  - **比例选择**：1:1（1024×1024）、4:3（1024×768）、16:9（1024×576）、9:16（576×1024）
  - **质量选择**：标清/高清/超清（具体映射由后端定义）
- **生成按钮**：输入框右侧的发送按钮（图标按钮），点击或按 ⌘+Enter 触发生成
- **随机提示词按钮**：从内置的 10 条随机提示词中随机选一条填入输入框
- **优化提示词按钮**：调用已配置的文本模型对当前提示词进行优化/扩写（需先在模型管理页配置文本模型）

### 4.2 生成过程
- 任务进入全局队列（串行执行，避免 API 限流）
- 生成中：显示加载状态，可查看队列进度
- 生成成功：图片直接显示在输入区下方的结果区域
- 生成失败：显示错误提示信息

### 4.3 结果展示
- 显示生成的图片
- 图片上显示使用的模型、比例、质量标签
- 点击图片可查看大图（Lightbox 预览）
- 图片下方有操作按钮：
  - **保存到项目**：将该图片保存到一个已有项目或新建项目
  - **重新生成**：用相同参数重新生成
  - **下载/保存到本地**：将图片保存到本地文件系统
- 每次生成的图片同时记录到"生成历史"中

---

## 五、生成历史页

### 5.1 历史列表
- 以瀑布流/网格形式展示所有快速生图产生的图片（按时间倒序）
- 每张图片缩略图显示：生成时间、使用模型、比例
- 图片数量上限 200 条，超出自动淘汰最旧的

### 5.2 历史操作
- **点击图片**：打开大图预览（Lightbox）
- **删除单条**：删除某一条历史记录（含图片）
- **清空历史**：二次确认后清空所有历史记录

---

## 六、项目列表页

### 6.1 项目卡片
- 每个项目以卡片形式展示
- 卡片显示：项目封面图（手动钉选的图片优先，否则取最新生成图）、项目名称、项目描述（如有）、版本数、图片总数、最后更新时间（相对时间）
- 新建项目时：填写名称、描述（可选）、初始提示词、选择模型，创建后自动生成第一条主线版本

### 6.2 项目操作
- **点击项目卡片**：进入项目工作区
- **新建项目**：打开新建对话框，填写名称/描述/初始提示词/选择模型
- **删除项目**：二次确认后删除整个项目（所有版本和图片不可恢复）

---

## 七、项目工作区（核心页面）

### 7.1 页面结构
项目工作区分上下两部分：
- **上方：横向时间轴**（版本树可视化）
- **下方：详情面板**（当前选中版本的图片画廊 + 生图输入区）

### 7.2 横向时间轴

#### 7.2.1 布局逻辑
- 时间轴是一个**可横向滚动**的区域，根版本（主线节点）从左到右按创建时间排列
- 每个根版本（主线节点）下方可以派生子版本（分支节点），同一父版本下的子版本**横向平行排列**
- 子版本本身也可以继续向下派生子版本，形成多级分支树
- 节点之间有连接线，纵向连接线表示衍生关系，父子节点之间不与文字重叠

#### 7.2.2 节点内容
- 每个节点显示：该版本最新一张图片的缩略图
- 节点上叠加显示：
  - 版本标签（自动编号：主线 v1, v2, v3...；分支 v1.1, v1.2, v1.1.1...）
  - 版本名称（可自定义，默认取提示词前10个字）
  - 图片数量
- 当前选中的版本节点有高亮/选中状态
- 根版本节点右上角有**删除按钮**（删除该主线及其所有分支）

#### 7.2.3 节点交互
- **点击节点**：切换当前选中版本，下方详情面板加载该版本内容；切换时保留时间轴的横向滚动位置
- **点击节点上的缩略图**：打开大图预览
- **删除节点**：点击删除按钮，弹出确认对话框（提示将删除 X 个衍生节点、Y 张图片），确认后删除该节点及其整个子树；至少保留一个版本

#### 7.2.4 顶部信息栏
- 时间轴标题"探索时间轴"
- 统计信息：X 条主线 · Y 个分支 · 共 Z 张图
- "新主线"按钮：创建一条新的根版本（不基于任何父图，等同于全新提示词开始）

### 7.3 详情面板

#### 7.3.1 面板头部
- 版本名称（可点击编辑/修改）
- 版本类型标签：主线/分支
- 世代标签（v1、v1.1 等）
- 该版本的模型信息、创建时间
- 项目设置按钮、删除项目按钮
- 返回项目列表按钮

#### 7.3.2 父图缩略图区
- 如果当前版本是子版本（从某张图衍生而来），在输入区上方显示父图缩略图
- 父图缩略图可点击，点击后打开大图预览查看父图详情
- 有文字标注说明这是参考图/父图

#### 7.3.3 图片画廊
- 以网格形式展示当前版本下所有已生成的图片，按时间倒序排列
- 每张图片卡片显示：图片缩略图、比例、质量、生成时间
- 图片操作：
  - **点击图片**：打开 Lightbox 大图预览
  - **衍生按钮**（图片上悬浮/叠加）：点击后以该图作为父图，在当前版本下派生出一个子版本，使用当前输入框的提示词进行图生图
  - **设为封面**：将该图片设为项目封面（列表页卡片展示的图片）
  - **删除图片**：删除该张图片
- 如果当前版本正在生成（队列中有任务），在画廊末尾显示占位/加载卡片

#### 7.3.4 生图输入区
- 与快速生图页的输入区结构一致（提示词输入框、粘贴图片、模型/比例/质量选择器、发送按钮）
- 区别：
  - 若当前是**根版本（主线）**：修改提示词或模型后点击生成，会**自动创建新的主线节点**并在新节点下出图（时间轴向右延伸新节点）
  - 若当前是**子版本（分支）**：修改提示词后点击生成，直接在当前子版本下出图（不创建新节点）
- 支持粘贴图片到输入框作为参考图（图生图）
- 支持随机提示词、优化提示词

### 7.4 大图预览（Lightbox）
- 所有页面的图片点击后都可打开 Lightbox
- Lightbox 功能：
  - 居中显示大图
  - 显示图片信息（提示词、模型、比例、质量、生成时间、所属版本）
  - 支持缩放（滚轮）
  - 点击外部区域或关闭按钮关闭
  - 在项目详情中打开时，如果有兄弟图片，支持左右切换

### 7.5 项目设置对话框
- 修改项目名称
- 修改项目描述
- 保存生效

---

## 八、设置页

### 8.1 关于信息
- 应用 Logo、应用名称"妙生 · miaos"
- 当前版本号
- 应用简介文字
- 开发模式下显示"开发模式"标签

### 8.2 软件更新
- 版本状态标签：尚未检查 / 正在检查更新… / 发现新版本 / 当前已是最新版本 / 下载中 / 已就绪，重启安装 / 更新出错
- 最新版本号
- 最新版本发布日期
- 下载进度条：显示百分比、下载速度
- 更新说明：渲染 Markdown 格式的 release notes（支持标题、列表、代码块、链接、加粗、斜体、引用、分隔线等）
- 错误提示区域（更新失败时显示错误原因）
- 按钮流程：
  1. **检查更新**：点击后向 GitHub Releases 检测最新版本
  2. **下载更新**（发现新版本后显示）：点击开始下载增量包
  3. **重启并安装**（下载完成后显示）：点击后退出应用并安装更新，安装后自动重启
- 应用启动后 5 秒自动检测更新，若发现新版本在右上角弹出 toast 通知提示用户前往设置页下载
- 更新源固定为 `https://github.com/dick86114/miaos` 的 GitHub Releases

### 8.3 偏好设置
- 快捷键说明列表（⌘Enter 快速生图、⌘R 刷新页面）

---

## 九、全局任务队列

- 所有生图请求（快速生图 + 项目生图）统一进入**全局串行队列**
- 队列任务状态：排队中（queued）→ 生成中（running）→ 已完成（done）/ 失败（failed）/ 已取消（canceled）
- 同一时间只执行一个任务，完成后自动执行下一个，避免 API 限流
- 排队中的任务可以取消（移除队列）；正在执行的任务不可取消（API 请求已发出）
- 切换页面不丢失队列进度，各页面通过订阅机制实时获取队列状态更新
- 项目生图中：根版本 prompt/model 变更触发自动建新主线节点；子版本在当前节点下追加图片
- 队列完成后通过 toast 通知用户结果（成功/失败）

---

## 十、全局提示系统（Toast）

- 操作成功/失败/信息提示，在页面右上角弹出
- 类型：成功（绿色）、错误（红色）、信息（蓝色）、警告（橙色）
- 自动消失（约 3 秒）
- 关键提示如"发现新版本""新版本已下载""参考图已粘贴"等

---

## 十一、数据持久化

- 所有配置和数据存储在浏览器 localStorage（key: `miaos.state.v4`）
- 持久化内容：
  - 供应商配置（含 API Key、模型启用状态）
  - 快速生图历史（最多 200 条）
  - 项目数据（含版本树、图片路径、提示词等）
  - 文本模型配置
  - 最近一次生图参数（上次使用的模型/比例/质量，下次打开自动恢复）
- 生成的图片文件保存在本地用户数据目录，通过 `file://` 协议加载
- 支持从旧版本数据格式自动迁移（v3 → v4）
- 粘贴的图片临时保存在系统临时目录

---

## 十二、文件系统/系统交互（主进程能力）

- **生图 API 调用**：主进程发起 HTTP 请求到配置的 API endpoint，下载生成的图片到本地
- **剪贴板读取**：支持从系统剪贴板读取图片数据（粘贴图片功能）
- **本地文件保存**：将剪贴板图片保存为临时文件
- **自动更新**：通过 electron-updater 检测、下载、安装 GitHub Releases 上的新版本
- **文件保存对话框**：将生成的图片保存到用户指定位置
- **图片打开**：在系统默认图片查看器中打开图片

---

## 十三、参数选项汇总

### 13.1 图片比例
| 选项 | 实际像素 |
|---|---|
| 1:1 | 1024×1024 |
| 4:3 | 1024×768 |
| 16:9 | 1024×576 |
| 9:16 | 576×1024 |

### 13.2 图片质量
- 标清 / 高清 / 超清（三档，具体参数由后端 API 定义）

### 13.3 内置 Grsai 模型列表
gpt-image-2、gpt-image-2-vip、nano-banana、nano-banana-fast、nano-banana-2、nano-banana-2-cl、nano-banana-pro、nano-banana-pro-vt、nano-banana-pro-cl、nano-banana-pro-vip（共 10 个，默认启用 3 个）
