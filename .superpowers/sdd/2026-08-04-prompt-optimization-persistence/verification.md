# 提示词优化状态与碎片动效验收记录

- 验收日期：2026-08-04
- 验收分支：`codex/foundation-security`
- 验收边界：未修改供应商密钥、供应商配置、`~/.miaos/` 或任何现有本地用户数据。

## 自动化已验证

命令：

```bash
pnpm check && git diff --check
```

结果：退出码 `0`。`pnpm check` 执行 `node --test tests`，共 `198` 项通过、`0` 项失败、`0` 项取消、`0` 项跳过；随后 `git diff --check` 无输出，确认没有差异空白格式错误。

自动化覆盖包含提示词优化状态机、快速页与项目页绑定、碎片 overlay、模型 chip 响应式/动效样式契约及其回归。该结论仅代表 Node 自动化测试与源码契约，不代表桌面窗口中的实际视觉或供应商网络调用。

## Electron 启动已验证

为避免触碰真实用户数据，使用临时 `HOME` 运行受控启动探测：

```bash
HOME="$(mktemp -d /tmp/miaos-task4-home.XXXXXX)" pnpm start
```

结果：`pnpm start` 在 8 秒观察期内持续运行，启动日志仅包含 `electron .`，未出现运行时错误；启动前后确认其启动的 `pnpm` 与 Electron 子进程均已终止，无残留进程。临时 HOME 未产生文件，未使用用户的 `~/.miaos/`。

此项仅证明 Electron 可由现有启动脚本拉起并稳定驻留 8 秒；限时终止是受控探测的预期结束方式，不是应用故障。

## 实际 GUI 与真实文本模型配置：未验证

以下项目**没有**在真实桌面 GUI 中操作，不能标记为已验证：

- 快速页和项目页中超长模型名称的实际截断、悬浮提示和生成按钮布局。
- 优化进行中切换到其他 Hash 路由再返回后的视觉恢复。
- 使用真实文本模型时，优化成功与失败后输入框、按钮、碎片层和 Toast 的实际清理。
- 窄窗口下生成按钮不被挤压的真实布局。
- macOS 系统“减少动态效果”开启时的静态碎片状态。

原因：本次验收未修改或使用供应商密钥与供应商配置，也未读取或改写用户本地数据；无已配置且可安全调用的真实文本模型路径可供端到端验证。

## 计划勾选规则

- Task 1 至 Task 3 的步骤已依据对应实现报告和已存在的提交勾选。
- Task 4 的自动化验证和验收记录步骤已完成并勾选。
- Task 4 的 Electron 验收步骤保留未勾选：虽然已完成隔离的受控启动，但其列出的人工 GUI 验收项目尚未执行。
