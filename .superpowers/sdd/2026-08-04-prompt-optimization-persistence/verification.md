# 提示词优化状态与碎片动效验收记录

- 验收日期：2026-08-04
- 验收分支：`codex/foundation-security`
- 验收范围：仅执行文档更新、自动化检查与受控启动观察；未修改供应商密钥、供应商配置、`~/.miaos/` 或现有本地用户数据，未发起真实文本模型请求。

## 自动化已验证

执行命令：

```bash
pnpm check && git diff --check
```

结果：退出码 `0`。

- `pnpm check` 调用 `pnpm test`，后者执行 `node --test tests`。
- Node 测试共 `198` 项，`pass 198`、`fail 0`、`cancelled 0`、`skipped 0`、`todo 0`，耗时 `2243.181916ms`。
- 随后的 `git diff --check` 无输出，未发现差异中的空白格式错误。

此结果验证 Node 自动化测试与源码契约；不等同于 macOS 桌面窗口中的实际视觉、交互或供应商网络调用验收。

## 启动已验证

启动前先执行：

```bash
pgrep -af Electron
```

发现上一位执行者遗留了本工作树的 `pnpm start` / Electron 进程树，已先以 `SIGTERM` 受控终止并复核本工作树无残留进程，避免重复实例。系统未提供 `timeout` 命令，因此按约定使用等价回退方式：后台启动、等待 8 秒、再按进程树发送 `SIGTERM`。

```bash
pnpm start >"$log_file" 2>&1 &
start_pid=$!
sleep 8
# 递归终止 start_pid 的子进程后终止 start_pid
```

结果：启动 PID `10674` 在完整 8 秒观察期后仍在运行；日志仅包含既有启动脚本输出 `electron .`，未出现运行时错误。随后主动终止该启动进程树，`wait` 退出码为 `0`，并再次确认没有指向本工作树的 Electron 进程残留。

**启动观察已完成，非应用启动失败。** 主动终止是受控时限观察的预期结束方式，不能按非零退出码或常驻行为判定为启动失败。

## GUI 与真实文本模型：未验证

以下项目没有在真实桌面 GUI 中操作，不能标记为已验证：

- 快速页与项目页的超长模型名称实际截断、悬浮提示和生成按钮布局。
- 优化过程中切换到其他 Hash 路由并返回后的实际状态恢复。
- 真实文本模型成功或失败后输入框、按钮、碎片层与 Toast 的实际清理。
- 窄窗口下生成按钮不被挤压的真实布局。
- macOS 系统“减少动态效果”开启时碎片状态的实际呈现。
- 供应商网络可达性、凭据有效性和真实提示词优化输出质量。

原因：本次任务不修改供应商密钥、供应商配置或本地用户数据，也未对真实 GUI 执行人工交互，更未安全地调用已配置的真实文本模型。常规 `pnpm start` 启动观察可能读取应用既有运行时配置，但本任务未查看、写入或变更其中任何密钥、配置或用户数据。

## 计划勾选规则

- Task 1 至 Task 3 的步骤已依据对应实现报告和既有提交勾选。
- Task 4 Step 1 与 Step 3 已完成并保持勾选。
- Task 4 Step 2 已完成限时启动观察，但因其要求的人工 GUI 与真实文本模型验收均未执行，保留未勾选。
