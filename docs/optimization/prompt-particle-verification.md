# 提示词粒子动效验证记录

- 验证日期：2026-08-04
- 验证工作树：`/Users/dickies/Documents/workspaces/miaos/.worktrees/foundation-security`
- 运行版本：通过 `pnpm start` 启动的开发版 Electron（仅保留一个该工作树的主进程）。

## 自动化验证

- `pnpm check`：已通过，Node 测试共 165 项，通过 165 项，失败 0 项。
- 相关覆盖已纳入测试套件：
  - `tests/generate-ui-contract.test.mjs` 校验快速生成页与项目工作台均使用输入区内的粒子层、移除旧 `.composer-wave-bar`、在请求前激活粒子层，并保证粒子层位于文字下方且不拦截交互。
  - `tests/motion-style.test.mjs` 校验粒子动画只使用允许的合成属性，并要求 `prefers-reduced-motion: reduce` 下粒子根层保持可见且所有粒子/伪元素动画停止。
- 本次只读复审检查了两处优化生命周期：`src/js/pages/generate.js` 与 `src/js/pages/project.js` 都在调用 `optimizePrompt()` 前添加 `is-optimizing`，并在 `finally` 中移除按钮、输入框和粒子层状态；因此成功与捕获到的失败路径都具有清理代码。该结论是静态代码复审，不替代真实供应商请求下的 GUI 验收。

## 已完成的 GUI 检查

- 已按任务要求终止该工作树旧 Electron 进程，并通过 `pnpm start` 启动最新开发版；进程检查确认仅有一个来自 `codex/foundation-security` 的 Electron 主进程。
- 在快速生成页输入了有效提示词“`一只橘猫在雨夜的霓虹街头，电影感，高清插画`”，输入框可正常显示文字与获得焦点。
- 点击“优化提示词”后，应用实际显示“请先在「设置 → 模型供应商」中配置文本模型”。因此本次没有发起文本供应商请求，粒子层也不会进入优化状态。
- 已打开“项目”页；当前本地数据没有项目，无法进入项目工作台输入框执行相同操作。

## 尚待用户/真实供应商验证

以下项目**均未标记为已通过**：

1. 在已配置可用文本供应商且可进入项目工作台的环境中，分别在快速生成页和项目工作台输入有效提示词并实际触发“优化提示词”。
2. 在真实请求进行期间确认粒子覆盖整个输入框范围，文字与光标仍可读，且页面不出现底部频谱条。
3. 分别验证真实请求成功结束和供应商失败结束后，粒子层、输入只读状态及按钮加载状态都会完全消失。
4. 开启系统“减少动态效果”后，以真实请求验证只保留静态彩色粒子提示，不播放粒子或渐变漂移动画。

限制说明：本机当前未配置文本模型，且项目列表为空；未实际触发文本供应商。因此本记录不声称粒子动效、清理效果或减少动态效果的人工 GUI 检查已经通过。
